1.String k-v 单个key最大能存512MB，约等于 多个文字呢？
增(修改):set key value  例子:set adminName ‘zhangsan’
查:get key  例子:get adminName
查询所有的key:key *    例子:keys *
删key:del key   例子:del adminName

一、String独有的命令
(重要)过期时间:setex 存的时候直接给定过期时间 setex code 60 2356 先时间后value
(重要)自增(减):incr  应用场景:抖音点赞,产品的热销排行 自增的值一定是数字
定量增:incrby    incrby mycode 30
自减:decr key
定量减:decrby key
批量增:mset key1 value key2 value
批量查:mget key1 key2
字符串截取:getrange key start end
先查再存:getset  格式 getset key value
将给定的key值设置value,并返回key的旧值
字符串长度:strlen key 的长度 ,不存在的长度为0
字符串的追加:append key value 追加的位置是value的末尾

其他的通用命令
1.DUMP 给定key中的值进行 序列化回值是 序列化后的值  了解***
2.exists key值是否存在，很常用 返回值是1或者0,1代表存在，0代表不存在
3.exprire 给key 做一个过期时间 写的值是 过期的秒数 例子: set phoneCode 3343 expire phoneCode 60
                    到期之后key自动删除，这个很常用！！
4.ttl 查询 还剩多少时间过期 秒为单位,应用场景,1)订单15分钟有效 2)考试
5.persist 移除key的过期时间,将key保持为持久状态  开票
6.rename 更改key值  不常用
7.renamenx 当key新的key不存在是,改为新名字
8.type key 返回key的数据类型---5种
9.randomkey 随机出key 抽奖常用

二、Hash 哈希 类型 用于存储对象
增: hset 例子:hset food name 'apple'
删: del food name
批量增: hmset food name 'apple' color 'red'
增: hget food color
查询所有的key: hkeys
查询所有的key和value: hgetall
hash字段的自增:hincrby human age 10
key person value 一堆k v组合
hash数据类型的遍历:Hscan 不常用

三、list----双端队列结构  先进后出 排序是按照插入的顺序进行排序,有顺序且能重复
比如:  老沙  八戒  猴子  盲僧     妲己  刘备 关羽  张飞  赵云
左边增: lpush key xxx
右边增: rpush key xxx
左删除: lpop key
右删除: rpop key
遍历: lrange key 0 -1
插入: linsert key before '刘备' '妲己'
单查: lindex key 下标
删除指定的值: lrem key count value
count>0 表示从头到尾,从左到右搜索,移除和value相等元素,数量为count
count<0 表示从尾到头,从右到左搜索,移除和value相等元素,数量为count的绝对值
count=0 表示移除表中所有与value相等的值
注意:因为是list的数据类型,所以值是可以相同的,比如两个xxx,两个xxx

四、set数据类型,set无序,不能重复
增: sadd key value
查条数: scard key  获取key的成员数量
查询全部: smembers key 返回集合中的所有value
按索引查询??????  无 sindex names 2 没有的......因为没有顺序
指定删除: srem key value
随机删除: spop key
随机查: srandmember key

五、zset sortest,有序的set集合,可以有序但是不能重复
它是set的扩展版,每个元素都关联了一个double类型的分数,根据分数的大小,进行排序,排序规则是从小到大
分数可以并列即分数可以重复,成员(value)必须唯一
底层是哈希表实现的,添加删除很快
增加: zadd key 分数 值     pnames
遍历: zrange key 0 -1 withscores 加上withscores带分数,不带直接显示全部
查条数: zcard key
指定删除: zerm key value  移除集合中的一个或者多个成员


以上就是redis常用的命令!!!!!!还有很多不常用的命令,自己去菜鸟网和redis去学习



